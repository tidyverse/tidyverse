# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

addons:
  apt:
    packages:
      - gcc-4.4
      - g++-4.4
      - clang

env:
  global:
    - CC=gcc-4.4 CXX=g++-4.4
    - CC=gcc     CXX=g++
    - CC=clang   CXX=clang++
